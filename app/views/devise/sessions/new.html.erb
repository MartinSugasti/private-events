<div class="login-container d-flex justify-content-center align-items-center">
  <div class="p-5 bg-primary rounded login-square">
    <div class="text-center m-auto w-90">
      <h1 class="px-3 mb-3 login-logo text-secondary">Private Events</h1>
    </div>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field mt-5 form-group form-icons">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Email', class: 'pl-2 py-3 bg-primary rounded w-100 border email-field pr-5' %>
      </div>

      <div class="field mt-3">
        <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Password', class: 'pl-2 py-3 bg-primary rounded w-100 border password-field pr-5' %>
      </div>

      <div class="actions mt-4 mb-2 text-center">
        <%= f.submit 'Log in', class: 'login-button bg-tertiary py-3 px-4 mt-2 border-0 rounded-pill text-uppercase text-light' %>
      </div>
    <% end %>

    <div class="text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
